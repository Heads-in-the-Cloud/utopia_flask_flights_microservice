version: "3.8"
services:
  flights: 
    build:
      context:
        ./flights_api
  mysqlserver:
    build:
      context:
        ./mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=
      - MYSQL_ALLOW_EMPTY_PASSWORD=true
      - MYSQL_USER=
      - MYSQL_PASSWORD=
      - MYSQL_DATABASE=
    volumes:
      - mysql_data:/var/lib/mysql
      - mysql_config:/etc/mysql/conf.d
      - ./mysql:/home

volumes:
  mysql_data:
  mysql_config: